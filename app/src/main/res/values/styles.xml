<resources>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <!--定义ProgressBar大小-->
    <style name="LoadingProgressStyle" parent="@style/Widget.AppCompat.ProgressBar">
        <item name="android:minWidth">20dp</item>
        <item name="android:maxWidth">20dp</item>
        <item name="android:minHeight">20dp</item>
        <item name="android:maxHeight">20dp</item>
        <item name="android:indeterminateTintMode">src_atop</item>
        <item name="android:indeterminateTint">@color/colorPrimary</item>
    </style>
    <!-- ToolBar菜单样式 -->
    <style name="ToolBarStyle" parent="@style/BaseToolBarStyle">
        <item name="actionMenuTextColor">@color/colorWhite</item>
    </style>

    <style name="TabLayoutTextStyle">
        <item name="android:textSize">@dimen/sp_16</item>
    </style>

    <style name="roomRatingBar" parent="@style/Widget.AppCompat.RatingBar.Small">

        <item name="android:progressDrawable">@drawable/ratingbar_smile</item>
<!--
        <item name="android:minHeight">@dimen/dp_32</item>

        <item name="android:maxHeight">@dimen/dp_32</item>-->

    </style>

</resources>
